<UserControl x:Class="Biblioteca.Controls.Totais"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Biblioteca.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Background="White">
        <materialDesign:Card Padding="32" Margin="16" Background="White">
            <StackPanel Margin="15 0 0 0">
                <TextBlock Text="{Binding Entidade}" Foreground="Black"/>
                <TextBlock Text="{Binding Valor}" FontWeight="Bold" Foreground="Black" FontSize="20"/>
            </StackPanel>
        </materialDesign:Card>
        
    </Grid>
</UserControl>
